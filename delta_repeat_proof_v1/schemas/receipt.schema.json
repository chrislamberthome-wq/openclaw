{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "receipt.schema.json",
  "title": "ExecutionReceipt",
  "description": "Verifiable execution receipt proving governed decision, constraint adherence, and reproducible result.",
  "type": "object",
  "required": ["cycle_id", "trace_sha256", "replay_match", "governance_verdict", "status"],
  "additionalProperties": false,
  "properties": {
    "cycle_id": {
      "type": "string",
      "description": "Identifier for the execution cycle."
    },
    "trace_sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "SHA-256 of the entire canonical trace file."
    },
    "replay_match": {
      "type": "boolean",
      "description": "True if replaying the task produces the expected output."
    },
    "governance_verdict": {
      "type": "string",
      "enum": ["ALLOW", "DENY"],
      "description": "Governance verdict recorded in the trace."
    },
    "status": {
      "type": "string",
      "enum": ["PASS", "FAIL", "ERROR"],
      "description": "Overall verification status; PASS only when all checks succeed."
    }
  }
}
